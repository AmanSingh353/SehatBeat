# Example environment file for frontend
# Copy to .env and fill in real values. Do NOT commit .env.

# Perplexity API key (rotate if previously exposed)
# If your app uses the key on the frontend, prefix with VITE_
# Note: Exposing API keys in the frontend is insecure. Prefer proxying via backend.
VITE_PERPLEXITY_API_KEY=

# Backend API base URL for frontend to call
VITE_API_BASE_URL=http://localhost:8787
